export type TrendItem = {
  id: string;
  label: string;
  posts: string;
};

export type DailyQuestItem = {
  id: string;
  label: string;
  completed: boolean;
};

export type SuggestedPlayer = {
  id: string;
  name: string;
  username: string;
  initials: string;
};