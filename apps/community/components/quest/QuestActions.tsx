"use client";
import{CheckCircle2,Gift,LoaderCircle}from"lucide-react";import{useRouter}from"next/navigation";import{useEffect,useRef,useState,useTransition}from"react";import{claimDailyCommunityQuest,recordDailyQuestVisit}from"@/app/actions/community/quests";
export function QuestVisitRecorder(){const once=useRef(false);const router=useRouter();useEffect(()=>{if(once.current)return;once.current=true;void recordDailyQuestVisit().then(r=>{if(r.changed)router.refresh();});},[router]);return null;}
export function ClaimHarmonyButton({questCode,ready,claimed,reward}:{questCode:string;ready:boolean;claimed:boolean;reward:number}){const[pending,startTransition]=useTransition();const[message,setMessage]=useState("");const router=useRouter();return <div className="shrink-0"><button type="button" disabled={!ready||claimed||pending} onClick={()=>startTransition(async()=>{const result=await claimDailyCommunityQuest(questCode);setMessage(result.message);if(result.ok)router.refresh();})} className="inline-flex min-h-11 w-full items-center justify-center rounded-full bg-[#4f8124] px-4 text-sm font-black text-white disabled:cursor-not-allowed disabled:bg-[#b8c8a8] sm:w-auto">{pending?<LoaderCircle className="mr-2 animate-spin" size={17}/>:claimed?<CheckCircle2 className="mr-2" size={17}/>:<Gift className="mr-2" size={17}/>} {claimed?"Claimed":`Claim +${reward}`}</button>{message?<p role="status" className="mt-2 max-w-48 text-xs font-bold text-[#7a5635]">{message}</p>:null}</div>}
