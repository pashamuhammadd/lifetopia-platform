"use client";
import{LoaderCircle,MessageCirclePlus}from"lucide-react";import{useRouter}from"next/navigation";import{useState,useTransition}from"react";import{startDirectConversation}from"@/app/actions/community/messages";import{Card}from"@/components/ui/Card";
export function NewConversationForm(){const[pending,startTransition]=useTransition();const[message,setMessage]=useState("");const router=useRouter();return <Card className="p-5"><form action={formData=>startTransition(async()=>{const result=await startDirectConversation(formData);setMessage(result.message);if(result.ok&&result.conversationId)router.push(`/messages/${result.conversationId}`);})}><label className="font-black text-[#2f2418]" htmlFor="message-username">Start a conversation</label><div className="mt-3 flex flex-col gap-2 sm:flex-row"><input id="message-username" name="username" required minLength={3} maxLength={32} placeholder="Lifetopia username" className="min-h-12 min-w-0 flex-1 rounded-[16px] border border-[#ead9b8] bg-[#fffaf0] px-4 font-bold"/><button disabled={pending} className="inline-flex min-h-12 items-center justify-center gap-2 rounded-full bg-[#4f8124] px-5 font-black text-white">{pending?<LoaderCircle size={18} className="animate-spin"/>:<MessageCirclePlus size={18}/>}Message</button></div>{message?<p role="status" className="mt-2 text-sm font-bold text-[#7a5635]">{message}</p>:null}</form></Card>}
