"use client";
import{CheckCircle2,Gift,LoaderCircle}from"lucide-react";import{useRouter}from"next/navigation";import{useState,useTransition}from"react";import{claimWalletHarmonyBonus}from"@/app/actions/community/wallet";
export function WalletBonusButton({claimed}:{claimed:boolean}){const[pending,startTransition]=useTransition();const[message,setMessage]=useState("");const router=useRouter();return <div><button type="button" disabled={claimed||pending} onClick={()=>startTransition(async()=>{const result=await claimWalletHarmonyBonus();setMessage(result.message);if(result.ok)router.refresh();})} className="inline-flex min-h-12 w-full items-center justify-center rounded-full bg-[#4f8124] px-6 font-black text-white disabled:cursor-not-allowed disabled:bg-[#b8c8a8] sm:w-auto">{pending?<LoaderCircle className="mr-2 animate-spin" size={18}/>:claimed?<CheckCircle2 className="mr-2" size={18}/>:<Gift className="mr-2" size={18}/>} {claimed?"Bonus claimed":"Claim +500 Harmony"}</button>{message?<p role="status" className="mt-3 text-sm font-bold text-[#7a5635]">{message}</p>:null}</div>}
