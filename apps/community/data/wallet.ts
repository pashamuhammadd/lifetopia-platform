import { createClient } from "@repo/lib/supabase/server";
type WalletRow={id:string;address:string;linked_at:string;last_verified_at:string};
type AccountRow={points:number|string;level_floor:number};
export type CommunityWalletStatus={wallet:{id:string;address:string;linkedAt:string;lastVerifiedAt:string}|null;bonusClaimed:boolean;harmonyPoints:number;harmonyLevel:number};
export async function getMyCommunityWallet():Promise<CommunityWalletStatus>{const supabase=await createClient();const[{data:walletData},{data:bonusData},{data:accountData}]=await Promise.all([supabase.from("account_wallets").select("id,address,linked_at,last_verified_at").eq("chain","solana").maybeSingle(),supabase.from("harmony_ledger").select("id").eq("source_type","wallet_verification").limit(1).maybeSingle(),supabase.from("harmony_accounts").select("points,level_floor").maybeSingle()]);const row=walletData as WalletRow|null;const account=accountData as AccountRow|null;const points=Number(account?.points??0);return{wallet:row?{id:row.id,address:row.address,linkedAt:row.linked_at,lastVerifiedAt:row.last_verified_at}:null,bonusClaimed:Boolean(bonusData),harmonyPoints:points,harmonyLevel:Math.max(Number(account?.level_floor??1),Math.floor(points/500)+1)};}
