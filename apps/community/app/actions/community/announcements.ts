"use server";import{revalidatePath}from"next/cache";import{createClient}from"@repo/lib/supabase/server";
export async function publishCommunityAnnouncement(formData:FormData){const title=String(formData.get("title")??"").trim();const body=String(formData.get("body")??"").trim();if(title.length<4||title.length>120||body.length<10||body.length>2000)return{ok:false,message:"Check the announcement title and body length."};const supabase=await createClient();const{error}=await supabase.rpc("publish_community_announcement",{p_title:title,p_body:body});if(error)return{ok:false,message:"The announcement could not be published."};revalidatePath("/notifications");return{ok:true,message:"Official announcement published."};}
