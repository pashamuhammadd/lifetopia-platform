"use server";
import { revalidatePath } from "next/cache";
import { createClient } from "@repo/lib/supabase/server";
export type QuestActionResult={ok:boolean;message:string;awarded?:number;changed?:boolean};
export async function recordDailyQuestVisit():Promise<QuestActionResult>{const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return{ok:false,message:"Sign in to start today’s quests."};const{data,error}=await supabase.rpc("record_community_quest_event",{p_event_type:"daily_login",p_subject_id:null});if(error)return{ok:false,message:"Today’s quest visit could not be recorded."};const changed=Boolean(data);if(changed)revalidatePath("/quest");return{ok:true,changed,message:changed?"Daily visit recorded.":"Daily visit was already recorded."};}
export async function recordQuestPostView(postId:string):Promise<void>{if(!/^[0-9a-f-]{36}$/i.test(postId))return;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return;await supabase.rpc("record_community_quest_event",{p_event_type:"post_view",p_subject_id:postId});}
export async function claimDailyCommunityQuest():Promise<QuestActionResult>{const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return{ok:false,message:"Sign in before claiming Harmony."};const{data,error}=await supabase.rpc("claim_daily_community_quest");if(error)return{ok:false,message:error.message.includes("quest_incomplete")?"Complete every daily quest before claiming.":"Harmony could not be claimed."};const row=Array.isArray(data)?data[0]:data;const awarded=Number(row?.awarded??0);revalidatePath("/quest");revalidatePath("/my-world");return{ok:true,awarded,message:awarded?`+${awarded} Harmony claimed.`:"Today’s reward was already claimed."};}
