# Lifetopia Community Phase 10.1 — Android Studio

Overlay this package at the monorepo root. It creates `apps/community-android` with the permanent application ID `io.lifetopiaworld.community`, TWA launcher, verified HTTPS App Links, native Phantom/Solflare return bridge, Gradle wrapper, Android resources, verification script, and build/signing instructions.
